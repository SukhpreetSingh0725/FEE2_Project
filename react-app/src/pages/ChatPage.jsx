// src/pages/ChatPage.jsx

import React from 'react';

function ChatPage() {
  return (
    // Placeholder content for the Chat Interface
    <section id="chat-interface">
      <h1>SparkQ Chat</h1>
      <p>This is where the real-time chat functionality will be built using a **stateful component** (which we'll do next) to manage messages and user input.</p>
      {/* You will add chat input, message list, and button here later. */}
    </section>
  );
}

export default ChatPage;